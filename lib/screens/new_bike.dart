import 'package:flutter/material.dart';

class NewBike extends StatelessWidget {
  //form for adding a bike to activate and put into the list

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}